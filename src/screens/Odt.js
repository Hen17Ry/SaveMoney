import React from 'react';
import { View, Image, Text, TextInput, TouchableOpacity, StyleSheet, StatusBar } from 'react-native';
import { Ionicons } from '@expo/vector-icons';

export default function Odt() {
    return (
      <View style={styles.container}>
        <Text>ODT Screen</Text>
      </View>
    );
  }


  const styles= StyleSheet.create(
    {
      container: {}

    }
  )